<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.birdlink.collector">

    <!-- Android 应用入口配置，Capacitor 会把 Web 页面加载到原生 WebView 中。 -->
    <!-- 这行很重要：局域网 Flask 服务是 http 地址，必须允许明文流量。 -->
    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- 主 Activity：App 启动时进入这里。 -->
        <!-- 这些 configChanges 交给 Activity 自己处理，避免屏幕旋转时重建 WebView。 -->
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name="com.birdlink.collector.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <!-- 桌面图标启动入口。 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- 文件共享 Provider，供 Capacitor/AndroidX 安全访问应用文件。 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- 权限声明。 -->

    <!-- 访问电脑 Flask 服务和 ESP32 局域网接口需要网络权限。 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 手机端按住说话需要麦克风权限。 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
</manifest>
